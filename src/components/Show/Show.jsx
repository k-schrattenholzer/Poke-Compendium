import './Show.css';

export default function Show({ show }) {
//destructure props needed from show
// const { id, name, language, premiere, image, summary, url } = show;

  return (
    <div className='Show-Container'>
      {/* <img></img> */}
      {/* <p>{name}</p> */}
    
    </div>
  )
}
